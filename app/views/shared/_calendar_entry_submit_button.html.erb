<hr size="1" width="70%">

<% if @event.new_record? %>
   <%= submit_tag "Add event", :id => "calendar_submit_button" %>
<% else %>
   <%= submit_tag "Update", :id => "calendar_submit_button" %>
   
   <%= button_to "Delete", @event, :method => :delete, :id => "calendar_entry_delete" %>
<% end %>

