<%= form_for @style do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="label">
   New Style:
  </div>
  <div class="field">
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>
