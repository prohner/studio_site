<%= form_tag(master_data_show_term_groups_path, :method=>'get', :remote => :true) do %>
   <%= hidden_field_tag :master_style_id, @style.id %>
   <%= select_tag "master_federation_id", options_for_select(@federations.collect { |s| [s.name, s.id] }, @federation.nil? ? nil : @federation.id) %>
   <%= submit_tag "Choose a Federation" %>
<% end -%>

